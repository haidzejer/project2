<h1>Posts index</h1>
<article>
  <% @posts.each do |p| %>
    <div class="post">
      <h3><%= p.title %></h3>
      <h4> <%= link_to p.user.name, user_path(p.user) %> </h4>
      <h6> <%= time_ago_in_words(p.created_at) %> ago... </h6>
      <p><%= p.body %></p>
    </div>
  <% end %>
</article>
